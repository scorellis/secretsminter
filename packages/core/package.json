{
  "name": "@secretsminter/core",
  "version": "0.1.0",
  "description": "The value-blind minting core: the security invariants that never depend on the network. Holds the master keys; the agent never touches this directly.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build"
  }
}
