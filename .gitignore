node_modules/
dist/
coverage/
*.tsbuildinfo
.env
.env.*
*.log
.DS_Store

# secretsminter never persists secret material — but be defensive anyway
*.key
*.pem
secrets.json
.secretsminter/

# Local operator config for the daemon MCP server (keys, manifest, approvals) — never commit.
.secretsminter-local/
